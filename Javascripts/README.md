Exercise 1 : 
         create create <input/> type text . using the  .getAttribute("type") to get type from
       input and show it to console.
       note :  befor use getAttribute you need to select input element from html firt.
Exercise 2 : 
     - create <input/> type text . after when click button change the type at input 
       will be change to file.
     - modify code on exercise 1 : use condition to check 
        if <input/> type text change to file .
        if <input/> type file change to text.
Exercise 4 : 
      create <div> with two custom data attributes (data-id="contents" data-value="20%")
      using (dataset.id) to get value of data-id from div element and show value to console
Exercise 5 :
      - Create three <button> elements in HTML and identification a data-id attribute (e.g., home, about, contact) to store custom data.
      - using javascript selector : document.querySelectorAll("button") selects all <button> elements on the page and stores them in the buttons variable
      - uses forEach to iterate over the buttons and an event listener. ( btn.addEventListener("click", (e) => {} )
      - When a user clicks a button, the corresponding data-id value (e.g., "home") is printed to the browser's console
challenge Exercise :
**https://tabfilter-chanvisals-projects.vercel.app/**
